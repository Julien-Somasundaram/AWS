version: '3.7'

services:

  frontend:
    build:
      context: ../.
      dockerfile: test/dockerfile
    ports:
      - "5173:5173"
    # networks:
    #   - app-network

  
networks:
  app-network:
